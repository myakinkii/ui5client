{
  "name": "ui5client",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^4.4.0",
    "@capacitor/core": "^4.4.0",
    "@capacitor/ios": "^4.4.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.1",
    "@capacitor/cli": "^4.4.0"
  },
  "scripts": {
    "build": "ui5 build self-contained --all",
    "cap:assets": "npx @capacitor/assets generate --ios --android",
    "cap:sync": "cap sync",
    "cap:run": "cap run",
    "cap:open": "cap open"
  }
}
